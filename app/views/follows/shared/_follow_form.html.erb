<%= form_with model: @follow, local: true do |form| %>
    <%= form.hidden_field :follow_request, value: follow_request.id %>
    <%= form.hidden_field :followed_id, value: followed.id %>
    <%= form.hidden_field :follower_id, value: follower.id %>
    <%= form.submit "Accept" %>
<% end %>