<% if current_user %>
    <div style="display: flex; flex-direction: column; gap: 10px;">
        <div class=nav-bar-wrapper style="display: flex; justify-content: space-between; background-color: lightgray;">
            <div class="nav-bar-left-section">
                <%= link_to "Home", root_path unless request.path == root_path %>
                <%= link_to "Discover Users", users_path unless request.path == users_path %>
            </div>
            <div class="nav-bar-right-section">
                <%= link_to "My Profile", user_path(current_user) unless request.path == user_path(current_user) %>
                <%= link_to "Log Out", destroy_user_session_path, data: { turbo_method: :delete }  %>
            </div>
        </div>
    </div>
<% else %>
    <%= link_to "Log In", new_user_session_path  %>
    <%= link_to "Sign Up", new_user_registration_path %>
<% end %>